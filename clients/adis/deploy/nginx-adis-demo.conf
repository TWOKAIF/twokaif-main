server {
    listen 80;
    listen [::]:80;
    server_name adis-demo.twokaif.ru;

    root /var/www/adis-demo;
    index index.html;

    add_header X-Robots-Tag "noindex, nofollow, noarchive" always;

    location / {
        try_files $uri /index.html;
    }
}
